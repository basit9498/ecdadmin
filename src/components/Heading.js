import React from 'react'

const Heading = ({ text }) => {
    return (
        <>
            <h1 className='text-xl font-semibold text-primary'>{text}</h1>
        </>
    )
}

export default Heading